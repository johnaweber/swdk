name: Build and Deploy

on:
  push:
    branches:
      - main  # Change to the branch you want to build from

permissions:
  contents: write

jobs:
  build:
    name: Build and Deploy to Dist Branch
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install Dependencies
      run: yarn install

    - name: Build Project
      run: yarn build

    - name: Debug Build Output
      run: ls -la dist

    - name: Deploy to dist Branch
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
        git fetch origin
        git checkout -B dist
        # Delete node_modules directory
        rm -rf node_modules
        # Delete all files except .git and .github directories
        find . -not -name '.git*' -not -path './dist*' -delete
        # Debug: List the contents of the root directory
        ls -la
        # Copy build files from the temporary directory to the root
        cp -r dist/* .
        ls -la
        # Remove the temporary directory
        rm -rf dist
        ls -la
        cd $(pwd)
        # Check git status
        git status
        git add .
        git commit -m "Deploy to dist branch"
        git push origin dist --force
